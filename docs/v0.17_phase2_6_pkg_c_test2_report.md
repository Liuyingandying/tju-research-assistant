# v0.17 Phase 2.6-PKG-C — v0.17-test2 跨机器测试包报告

日期：2026-09-09  
范围：Responsive / HiDPI Hotfix 的 Windows x64 onedir 测试包

## 1. 结论

`PHASE_2_6_PKG_C = PASS`

- Version：v0.17-test2
- Packager：PyInstaller 6.16.0
- Mode：Windows x64 onedir、windowed/no-console
- EXE：`dist/TJU_Info_Retrieval_v0.17-test2/信息自动检索整理系统.exe`
- ZIP：`dist/TJU_Info_Retrieval_v0.17-test2_win64.zip`
- ZIP：87,598,655 bytes（83.54 MiB）
- ZIP SHA-256：`F18E85237005B21B05320B27AF4B34BA7E2DCCC68BB805BE3C53E30A5B7AD3DF`
- 未压缩目录：230,455,010 bytes（219.78 MiB）

## 2. test1 preservation

构建前后均确认以下产物存在：

- `dist/TJU_Info_Retrieval_v0.17-test1/`
- `dist/TJU_Info_Retrieval_v0.17-test1_win64.zip`

test1 ZIP SHA-256 在构建后仍为：

`01333B9125CA3585B71279BCF4608C7AA0BBFE296A0B877636BF36420A65D566`

`build_windows.ps1` 不再删除整个 `dist`；只清理 `build/<Version>`、本次版本目录和本次版本 ZIP。版本参数默认 `v0.17-test2`，并限制为测试版本格式，避免越界清理。

## 3. 版本与 packaging config

- `app_paths.PACKAGE_VERSION`：`v0.17-test2`
- spec 沿用既有 Playwright hook、PySide6 收集和外部 ICU 排除规则。
- spec 从 `TJU_PACKAGE_VERSION` 接收版本并生成对应 onedir 名称。
- `version.txt` 记录 test2、Windows x64 onedir、PyInstaller 6.16.0、Responsive / HiDPI hotfix、git commit，以及 UI Hotfix 已取得的正式 clean baseline：`1096 passed, 20 subtests passed, 0 failed, 0 errors`。
- 未修改 Query、Expansion、SearchService、来源 adapter、OfflineSummary、Evidence、Ranking、Filter、Portal 或 BrowserSession 业务行为。

构建命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\packaging\build_windows.ps1
```

构建成功，目录和 ZIP 均生成。

## 4. Responsive / HiDPI UI inclusion

最终冻结 EXE 内部使用正式 `MainWindow` 做几何 smoke：

- 920×560：ActionBar 自动两行，行数为 5+5。
- 920×560：人员单位输入框与匹配模式不相交。
- 920×560：10 个底部按钮均可见且不小于 minimum size hint。
- 920×560：结果表格 viewport 宽高均大于 0。
- 1920×1080：ActionBar 恢复单行。
- Demo 结果：7 条；正常关闭。

结果：`Responsive UI frozen smoke = PASS`。冻结构建与 PKG-B 源码态响应式行为一致，没有修改 UI 生产实现。

## 5. Targeted tests

按本阶段要求不重复追查 browser-heavy long-suite native crash，也没有再次运行完整 1000+ 套件。

执行：

```text
python -m pytest -q --basetemp <isolated> \
  tests/test_gui.py tests/test_app_paths.py \
  tests/test_artifact_source_routing.py tests/test_expansion_direction.py \
  test_poc_browser.py
```

结果：`136 passed in 37.29s`。

正式完整基线沿用 PKG-B 已取得的 clean run：`1096 passed, 20 subtests passed, 0 failed, 0 errors`。

## 6. Sensitive-data scan

构建脚本对 test2 目录和 ZIP 分别运行 `packaging/verify_dist.py`，最终复扫再次 PASS。

未发现：

- Cookies、Login Data、Web Data、storage state；
- `runtime/edge_profile` 或其它浏览器 profile 内容；
- token、password、Authorization header assignment；
- TJU 登录数据或开发机账号文件；
- 作为运行依赖嵌入的 `E:\AI_Workspace\TJU_Info_Retrieval`；
- tests、docs、runtime、build、`.git` 或 pytest cache。

ZIP 根目录唯一为 `TJU_Info_Retrieval_v0.17-test2/`。

## 7. Frozen、clean-directory 与 fresh-profile smoke

### 冻结 GUI

- 最终 test2 EXE 连续启动两次：窗口存在、可响应、标题正确。
- 两次均正常关闭，exit code 0；无 QThread destroyed crash。
- Offline Summary：PASS。

### Clean-directory

将完整 test2 onedir 复制到与项目无关的临时目录后启动：

- GUI / Qt / logo resource：PASS。
- Responsive UI / Offline Summary：PASS。
- 进程模块中来自仓库或 `E:\conda` 的模块数：0。
- Playwright driver 和系统 Edge 由该复制目录中的 EXE 成功运行。

结论：`Clean-directory = PASS`，不依赖源码或本机 Python 环境。

### Fresh-profile

使用隔离 `TEST_USER_DATA_ROOT`：

- `edge_profile`、`cache`、`logs` 自动创建；
- 系统 Microsoft Edge 检测、启动和关闭成功；
- 未携带或复用开发机 session；
- 未登录检索按预期返回“天津大学登录状态失效，请在受控浏览器中完成登录后重试”；
- 无 crash。

结论：`Fresh-profile = PASS`。

## 8. Source smoke

从非项目 clean-directory 中的最终 test2 EXE 发起，复用本机已恢复的受控 Edge profile 进行真实验证；该 profile 只用于本机验收，从未复制到 dist/zip。

| 链路 | 结果 | 证据 |
|---|---|---|
| CNKI Paper / 太赫兹 | PASS | 返回 1 条真实结果 |
| CNKI Patent / 太赫兹 | PASS | 返回 1 条真实结果 |
| 天津大学新闻网 / 太赫兹 | PASS | 返回 1 条真实结果 |
| IEEE Xplore / terahertz | PASS | 返回 1 条真实结果 |
| CNKI Paper Offline Summary | PASS | 结构化内容非空 |

检索走正式 `BrowserSession` 与 `SearchService`，没有建立第二套业务逻辑。

## 9. test1 与 test2

- test1：首次 Windows 跨机器功能测试版本，作为历史基线保留。
- test2：在 test1 同一检索功能基础上包含 Responsive / HiDPI UI Hotfix。
- test2 没有新增检索功能或改变 browser backend。

以后新测试者应收到：`TJU_Info_Retrieval_v0.17-test2_win64.zip`。test1 不再作为新测试首选，只保留用于历史对比。

## 10. 朋友测试说明

1. 发送 test2 ZIP，要求完整解压后运行，不要在 ZIP 内直接启动。
2. 测试者使用自己的 TJU 账号在受控 Edge 中登录。
3. 按 `docs/v0.17_cross_machine_test_checklist.md` 完成约十几分钟测试。
4. 界面问题同时记录 Windows 版本、分辨率、显示缩放和 Edge 版本。
5. 反馈可提供失败步骤、截图和最新日志；不得提供密码、Cookie、Token 或 Edge profile。

## 11. Known limitations

- 当前是未签名测试二进制，SmartScreen 可能提示未知发布者。
- 依赖 Windows 10/11 x64、系统 Microsoft Edge、网络和测试者本人的 TJU 访问权限。
- 本轮完成的是本机 dist 与非项目 clean-directory 模拟；不同物理机器的结果仍需朋友回传。
- PKG-B 已记录 browser-heavy 长套件的非确定性 Windows native crash；本阶段按要求没有重复扩大排查。
- 不需要 Python、pip、Git、Visual Studio 或管理员权限。
- 不进入 onefile、installer、NSIS 或 release polish。

最终状态：`PHASE_2_6_PKG_C = PASS`。
