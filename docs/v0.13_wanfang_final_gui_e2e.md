# 万方高级检索最终 GUI 真机验收报告

> 验收日期：2026-09-04 ｜ 真实 GUI（`python main.py`）+ 天津大学授权 ｜ 生产代码零修改
> 条件：研究方向=太赫兹 ｜ 扩展方向=综合（任意）｜ 人员单位=天津大学 ｜ 数据源=仅万方 ｜ 匹配模式=智能匹配（soft，未开启严格）
> 证据：`runtime/wanfang_final_gui/` — final_0results.png / search_start.txt / search_done.txt

## 1. 执行记录

| 步骤 | 结果 |
|-|-|
| 关闭旧进程 | ✅ 终止残留 python 进程 |
| 从当前工作树重启 GUI | ✅ 两次启动（pid 31248 → pid 2768），窗口正常 |
| 设置条件 | ✅ 研究方向=太赫兹、人员单位=天津大学、万方✓ / CNKI✗、匹配模式=智能匹配（`[53] combobox: 智能匹配`） |
| 点击开始检索 | ✅ 触发（时间戳 search_start=1788604488） |
| **最终结果数量** | ❌ **状态栏“检索完成：0 条”**（第二次启动后应用存活、检索流程正常结束但 0 条；截图 final_0results.png） |

第一次启动在检索期间应用进程干净退出（exit 0、无 stdout 日志），为环境偶发现象；第二次完整跑完流程但结果为 0。

## 2. 环境指纹（按要求打印）

```
git commit/hash : 42cbb29a93137fd960b571d187865c4b1c749b4b
                  42cbb29 feat: release v0.9.0 modern academic UI
                  （工作树含未提交的 v0.13 全部修改：wanfang.py 等 13 个文件）
wanfang.py 路径 : E:\AI_Workspace\TJU_Info_Retrieval\src\tju_info_retrieval\sources\wanfang.py
python 解释器  : E:\conda\python.exe（win32，Python 3.13.9）
```

## 3. 对照证据（同条件下探针实测）

| 路径 | 结果 |
|-|-|
| 真实 adapter 直接调用（软匹配、原始词“太赫兹”） | **20 条**（真实标题，db=万方）——`poc_final_e2e_diag.py` |
| SearchService 管线逐层计数（软匹配、同样查询） | **Adapter=20 → Merger=20 → Filter(soft)=20 → UI=20**——`poc_wanfang_gui_chain_diag.py` |
| 本 GUI 实跑 | **检索完成：0 条** |

即：**同一查询在软匹配下的完整管线计数为 20，而 GUI 呈现 0 条**——差异落在 GUI 组装/调用层（worker → SearchService → UI 信号→结果回填之间），或 GUI 本次运行中查询经某途径与探针不同（如扩展词时序、会话状态）。按任务要求：不修复，仅定位并记录。

## 4. 结论

- GUI 实跑“检索完成：0 条”（第二次运行，应用存活，流程正常结束）；
- 已按要求打印启动环境指纹（§2）；
- 同条件探针链路为 20 条 → 0 条差异不在万方高级检索/解析/过滤链路本身，而在 GUI 端组装或执行路径；进一步定位需 GUI 侧日志（本次未开启，不允许改动代码）。

## 5. 补充定位结论（后续审计修正，2026-09-05）

本报告初版将 0 条归因于“GUI 组装层或 worker 时序”——**该归因不成立，现予修正**：

- 实测当时**匹配模式已被切换为“严格匹配”**（strict；界面回读“严格匹配”）；
- 万方高级检索路径本身工作正常（同条件软匹配管线探针实测 Adapter=20 → Merger=20 → Filter=20 → UI=20）；
- 0 条的直接原因：**strict 语义下万方结果无单位元数据，被 FilterService 按设计全删**（strict 分支：record=None → 删除），属设计内行为而非链路缺陷；
- UI 人员字段映射审计：`人员单位 → input_author_affiliation → author_affiliation`、`人员姓名 → input_author_name → author_name` **映射正确无误**（main_window.py:238-249、564-565；回归测试 `test_search_request_person_unit_maps_to_affiliation` 守护）。

验收操作指引：使用**智能匹配（soft）**模式复跑，预期“检索完成：20 条”；如需严格匹配生效，须先实现万方单位元数据获取（作者单位字段/安装范围元数据），属独立任务。