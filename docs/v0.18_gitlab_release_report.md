# v0.18 RC GitLab 发布最终验收报告（Phase G）

- 日期：2026-09-17
- 任务：GitLab 发布封装与源码清理（Phase A–G）
- 结论：**发布封装完成，双作用域安全扫描 PASS，等待人工确认后上传。
  本任务未执行任何 git add / commit / push，未创建远程仓库。**

---

## 1. 文件统计

### 1.1 发布目录 `release/v0.18_RC/`（共 311 个文件，约 3.5 MB）

| 位置 | 文件数 | 内容 |
|---|---|---|
| `src/` | 94 | 主包全部源码与资源（含 tju_logo.png） |
| `tests/` | 87 | 正式测试套件（87 个文件，含 conftest 与隔离支持） |
| `docs/` | 114 | 精选后文档（排除 16 份个人数据/验收证据文档，见 §2） |
| `packaging/` | 4 | PyInstaller spec、构建脚本、hook、dist 校验脚本 |
| `tools/` | 4 | Release Guard、发布 runtime 准备、库清理工具 |
| `scripts/` | 1 | 验收检查脚本 |
| 根目录 | 7 | `main.py`、`conftest.py`、`requirements.txt`、`requirements-dev.txt`、`README.md`、`LICENSE`、`VERSION` |
| 类型分布 | — | Python 189、Markdown 115、其他 7 |

对清单的必要补充（相对任务书"包含"清单）：`main.py`（启动入口，README 安装方式依赖）、
`conftest.py`（测试路径引导）、`tools/` 与 `scripts/`（发布守护与验收工具，
使发布包可自检）。已在开发说明文档化。

### 1.2 明确排除（不进发布包）

`data/`（个人论文库+登录态）、`runtime/`（Edge Profile/日志/缓存/个人配置）、
`build/`、`dist/`、`x/`、根目录 58 个 `poc_*.py` 探针、`README_首次运行.txt`
（EXE 包专用）、生产启动根因报告、全部 `__pycache__` / `.pytest_cache`、
docs 中 16 份含个人数据或验收证据的文档。

## 2. 安全扫描结果

### 2.1 Phase A 全仓库审计（详见 `docs/v0.18_gitlab_release_audit.md`）

- 敏感词模式扫描（api_key 183、credential 125、edge_profile 106、storage_state 99、
  authorization 32、secret 52、password 28 等命中）逐类人工复核：**零真实密钥**；
  代码引用均为环境变量名 / keyring 接口 / 扫描规则 / 运行时内存注入。
- 真实密钥形态（sk- 前缀长串、JWT、Bearer 令牌值）：仅 `tests/test_release_data_scan.py`
  中的合成 fixture（该测试本身就是防泄漏守护）。
- git 历史 13 个提交按内容抽查：零命中。
- LLM 密钥链路：配置 JSON 只存非敏感字段，密钥走 keyring → DPAPI，仓库无 `credential.dat`。

### 2.2 Release Guard v2 双作用域终扫（`tools/check_release_data.py`）

| 作用域 | CRITICAL | HIGH | LOW | LOCAL | 判定 |
|---|---|---|---|---|---|
| dev（开发工作区 data/runtime/dist） | 0 | 0 | 1 | 10 | **PASS** |
| release（`--source-snapshot release/v0.18_RC`） | 0 | 0 | 0 | 0 | **PASS** |

- dev 的 LOCAL 项为开发机必须保留的登录态/论文库/画像（打不进发布包，仅本地存在）。
- release 首扫 FAIL 的 6 项（验收证据文档引用占位 key/测试标题 + 审计文档自身
  引用密钥形态字面量）已通过**排除 5 份文档 + 改写审计文档措辞**清零，无真实凭据涉事。
- 发布树 189 个 .py 全部 `compileall` 编译通过（结构完整性冒烟验证）。

### 2.3 个人数据反向扫描

以真实论文标题、个人备注、画像字段（THz-ISAC 等）为标记反扫发布目录：
docs 强标记清零；剩余命中仅为 UI 占位示例文案与提示词设计示例（顾问项，见 §5）。

## 3. Git 状态（详见 `git_check_report.md`）

- 分支 `poc1-cnki-metadata`，13 个提交；工作区 36 个已跟踪文件修改
  （v0.10→v0.18 业务演进，**早于本任务即存在**）+ 275 个未跟踪条目。
- `runtime/`、`data/` 下 git 可见文件仅 2 个白名单项（`summary_provider.example.json`、
  `data/.gitkeep`）；登录态/论文库/Profile/缓存/构建产物全部被 .gitignore 有效拦截。
- `.gitignore` 已按 Phase C 规范重写并逐条 `git check-ignore` 验证。
- **本任务未运行 `git add` / `git commit` / `git push`，未创建远程仓库。**

## 4. 发布结构

```
release/v0.18_RC/
├── LICENSE               （MIT，版权署名为项目贡献者，待人工确认）
├── README.md             （GitLab 主页：简介/功能/架构/安装/API 配置/开发说明）
├── VERSION               （v0.18 RC / Release Date: 2026-09）
├── main.py  conftest.py
├── requirements.txt  requirements-dev.txt
├── src/tju_info_retrieval/…   （94 文件）
├── tests/…                    （87 文件）
├── docs/…                     （114 文件，精选后）
├── packaging/…  tools/…  scripts/…
```

上传方式（二选一，人工决定）：① 整仓库提交后推送（按 git_check_report §4 决定
add 范围）；② 仅将 `release/v0.18_RC/` 内容作为仓库根初始化新 GitLab 仓库（最干净）。

## 5. 已知限制

1. **依赖拆分**：Word/PDF 导出所需的 `python-docx`、`reportlab` 目前在
   requirements-dev.txt；README 已注明基础安装外的可选安装步骤。未改 requirements.txt
   （属发布封装约束下的保守处理，建议后续版本合并运行时依赖）。
2. **UI 占位文案**：`library_window.py` / `settings_dialog.py` 的输入框示例含
   开发者研究方向色彩文案（THz-ISAC 等）。属业务代码，本次任务禁止修改，
   仅作为顾问项披露。
3. **LICENSE 选择**：任务书未指定许可证类型，本次按惯例采用 MIT 并署名项目贡献者；
   如学校有知识产权要求，需人工替换。
4. **开发仓库 docs/ 仍保留 16 份含个人数据/验收证据的文档**（已从发布目录排除）；
   若采用"整仓库推送"方式上传，需先人工决定这些文档是否 git add（默认不 add 即不会上传）。
5. **历史提交**：git 历史抽查无密钥/登录态，但若对历史彻底洁癖，可选"仅发布 release/
   目录新建仓库"方式，天然不含任何历史。
6. **版本号双轨**：`version.py` 的 `VERSION = "0.9.0"`（受测试断言保护）与
   `RELEASE_LABEL = "v0.18 RC"` 并存，为代码既有设计，本次未改动。
7. **测试全量回归**：本次封装未重新运行完整 pytest（Qt 用例在当前环境下需专用
   隔离配置）；发布目录已通过编译级结构验证。上传前建议人工执行一次
   `python -m pytest tests/`。

## 6. 任务合规声明

- 未修改任何业务代码：SearchService / Adapter / Evidence / SummaryService /
  ReportService / LLM Provider / LibraryRecord 零改动（本任务仅新增/修改：
  `.gitignore`、`README.md`、`VERSION`、`LICENSE`、`docs/v0.18_gitlab_release_audit.md`、
  `git_check_report.md`、`release/v0.18_RC/**`）。
- 未新增功能；未自动 push；未创建远程仓库；未上传 GitLab。
- 全部产物：`release/v0.18_RC/`、`.gitignore`、`README.md`、`VERSION`、`LICENSE`、
  `docs/v0.18_gitlab_release_audit.md`（Phase A）、`git_check_report.md`（Phase F）、
  本报告（Phase G）。
