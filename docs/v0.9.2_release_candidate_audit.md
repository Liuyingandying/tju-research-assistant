# v0.9.2 Release Candidate Audit（只读，未修改源码）

日期：2026-09-03

## 1. 当前 git diff 范围

**已跟踪修改（4 文件）**

| 文件 | 改动量 | 内容 |
| --- | --- | --- |
| `src/tju_info_retrieval/sources/ieee.py` | +24 | `parse_document_id` / `ieee_original_url` 纯函数 |
| `src/tju_info_retrieval/ui/main_window.py` | +88 / -8 | IEEE 原始站按钮 + 语言筛选接入 |
| `src/tju_info_retrieval/ui/theme.py` | +75 / -3 | 校徽资源读取 + 语言按钮 QSS 配色 |
| `.gitignore` | +4 | 根目录 `参考图.png` 忽略 |

**新增未跟踪（8 项，runtime/ 目录除外）**

| 文件 | 性质 |
| --- | --- |
| `src/tju_info_retrieval/ui/assets/tju_logo.png` | 校徽 PNG 资源（88×88，圆外透明） |
| `tests/test_ieee_original_url.py` | IEEE URL 解析 + GUI 按钮测试（13 用例） |
| `tests/test_language_filter.py` | 语言筛选测试（6 用例） |
| `docs/v0.9.1_language_filter.md` | 语言筛选实现文档 |
| `docs/v0.9.1_logo_fix.md` | 校徽替换实现文档 |
| `docs/v0.9.2_ieee_access_audit.md` | IEEE 访问审计文档 |
| `runtime/ui_preview_logo.py` | 校徽 UI 预览脚本（gitignore runtime/ 覆盖） |
| `runtime/lf_test_log.txt` 等 | 测试日志（gitignore runtime/ 覆盖） |

**参考图**：`参考图.png` 已加入 `.gitignore`，仍在磁盘未删除（待人工确认后清理）。

## 2. 敏感文件扫描

**结论：无敏感文件混入。**

- `runtime/` 目录已在 `.gitignore` 中，所有 runtime 产物（Edge Profile、缓存、日志、预览脚本）均被忽略。
- 工作区 `git status --short` 无 `.env`、`.cookie`、`.token`、`.credential` 等文件。
- `edge_profile/` 目录不在工作区变更中。
- `__pycache__/`、`.pytest_cache/`、`*.pyc` 均在 `.gitignore` 中。

## 3. version.py 同步状态

**版本仍为 `0.9.0`，未同步。**

- `src/tju_info_retrieval/version.py`：`VERSION = "0.9.0"`
- `README.md`：标注 `当前版本：**v0.9.0**`，FEATURES 列表含"现代化学术风格界面"。
- 本轮（v0.9.1 语言筛选 + v0.9.2 IEEE 双入口 + v0.9.1 校徽替换）未同步版本号。

**封版建议**：若本次作为一次发布，建议统一升级为 `0.9.2`，同步修改：
- `version.py`：`VERSION = "0.9.2"`
- `README.md`：版本标注 + FEATURES 新增两项（语言筛选、IEEE 双入口）
- `docs/v0.9.0_release_report.md` 中的 259 口径（v0.9.0 时点）不变
- README 测试结果行更新为 `278 passed`（或保留 259 标注为 v0.9.0 时点、新增一行 278）
- 测试断言文件（`tests/test_version.py`、`scripts/test_acceptance_check.py`）中硬编码的版本号

## 4. README 同步状态

**README 未同步本轮改动。**

当前 README 仅记录到 v0.9.0（现代学术风 UI），缺少：
- 语言筛选功能描述（zh/en/bilingual + 来源联动 + 状态保存）
- IEEE 双入口描述（"访问IEEE原始站"按钮 + 代理白屏回退）
- 校徽替换为真实天津大学校徽资源

FEATURES 列表现状（v0.9.0 时点）：
```
- 现代化学术风格界面：校徽 Header / 检索卡片 / 状态指示 / 空态引导（QSS 主题 + 矢量图标）
```
需新增：
```
- 语言筛选：中文 / English / 中英双语（默认双语），联动数据来源可用性，勾选状态保留
- IEEE 双入口：代理全文页面 + 原始站页面（代理白屏时稳定备用）
- 真实校徽：Header / 页脚使用从参考图提取的天津大学圆形校徽资源
```

## 5. 全量测试

`python -m pytest -q` → **278 passed**（34.33s）

- v0.9.0 基线：259
- v0.9.1 语言筛选：+6 = 265
- v0.9.2 IEEE URL：+13 = 278
- 无失败、无跳过。

**影响原有 259 测试**：无。全部 259 项包含在 278 中且全部通过。

## 6. 发布前问题清单

| # | 问题 | 严重度 | 说明 |
| --- | --- | --- | --- |
| P0 | 版本号未同步 | 高 | `version.py` 仍为 0.9.0，README 亦标注 0.9.0。封版需统一升级。 |
| P0 | README 未同步功能描述 | 高 | FEATURES 列表缺少语言筛选、IEEE 双入口、校徽替换三项。 |
| P0 | README 测试数标注过时 | 中 | 标注 259 passed（v0.9.0 时点），需新增 278 行或更新。 |
| P1 | `参考图.png` 未删除 | 低 | 已 gitignore，不影响提交；物理删除留待人工确认后执行。 |
| P1 | 测试断言硬编码版本号 | 中 | `tests/test_version.py`、`scripts/test_acceptance_check.py` 含 `VERSION` 断言，随 `version.py` 升级自动生效，无需单独改。 |
| P2 | `runtime/` 下临时文件 | 低 | 已被 `.gitignore` 覆盖（`runtime/` 整目录忽略），不影响提交。 |
| P2 | `docs/` 中多份 v0.9.1 文档 | 低 | `v0.9.1_logo_fix.md` + `v0.9.1_language_filter.md` + `v0.9.2_ieee_access_audit.md` 命名层级反映迭代过程；封版时若统一为 `v0.9.2` 文档命名可更清晰，但非阻塞。 |
| P3 | IEEE URL 正则非主机白名单 | 低 | `parse_document_id` 按 `/document/(\d+)` 路径匹配，非 IEEE URL 若恰好含此形态会被误重建——但 CNKI/万方无 `/document/` 路径，且有"无 document id 安全失败"兜底。 |

## 7. 建议封版提交范围

```
src/tju_info_retrieval/version.py          ← 版本号升级（若本次发布）
src/tju_info_retrieval/sources/ieee.py     ← IEEE URL 纯函数
src/tju_info_retrieval/ui/main_window.py   ← 语言筛选 + IEEE 原始站按钮
src/tju_info_retrieval/ui/theme.py         ← 校徽资源 + 语言按钮 QSS
src/tju_info_retrieval/ui/assets/tju_logo.png  ← 校徽 PNG 资源
README.md                                  ← 版本 + FEATURES + 测试数
.gitignore                                 ← 参考图忽略
tests/test_ieee_original_url.py            ← IEEE URL 测试
tests/test_language_filter.py              ← 语言筛选测试
docs/v0.9.1_language_filter.md             ← 语言筛选文档
docs/v0.9.1_logo_fix.md                    ← 校徽替换文档
docs/v0.9.2_ieee_access_audit.md           ← IEEE 访问审计文档
```

**禁止提交**：`runtime/`、`edge_profile/`、`.env`、`.pytest_cache/`、`__pycache__/`、`参考图.png`（已 ignore）。
