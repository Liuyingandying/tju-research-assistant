# v0.17-test2 正式封存归档报告

日期：2026-09-09
任务类型：RELEASE ARCHIVE（非开发/优化/重构）
封存位置：`F:\TJU_Info_Retrieval_Archive\`

---

## 1. 封存目标

将已验证通过的 Windows x64 测试版本 **TJU_Info_Retrieval_v0.17-test2** 完整归档至独立盘符，
包含可分发包（package）、解压运行目录（extracted）、源码快照（source_snapshot）、
阶段报告（reports）与校验和（checksums），并确保归档内容无任何敏感数据。

- 版本：v0.17-test2
- 状态：Release Candidate（冻结）
- 打包：PyInstaller 6.16.0，Windows x64 onedir
- 验证基线：1096 passed / 20 subtests passed / 0 failed / 0 errors

## 2. 目录结构

```
F:\TJU_Info_Retrieval_Archive\
└── v0.17_release_candidate\
    ├── package\          # 原始 ZIP（未重压缩）
    ├── extracted\        # 解压后的可运行目录
    ├── source_snapshot\  # 源码快照（不含敏感项）
    ├── reports\          # v0.17 阶段报告
    ├── checksums\        # SHA256 校验
    └── README_版本封存说明.txt
```

## 3. 文件清单

### package\
- `TJU_Info_Retrieval_v0.17-test2_win64.zip`（87,598,655 字节，复制自 dist/，未修改）

### extracted\
- `TJU_Info_Retrieval_v0.17-test2\`
  - `信息自动检索整理系统.exe`（3,640,027 字节）
  - `_internal\`（含 PySide6、playwright、MSVCP140/VCRUNTIME DLL 等完整运行时）
  - `README_首次运行.txt`
  - `version.txt`（v0.17-test2 / onedir / PyInstaller 6.16.0 / UI Hotfix / 1096 passed 基线）

### source_snapshot\（229 个文件）
- `main.py`、`README.md`、`README_首次运行.txt`、`requirements*.txt`
- `src\`（全部源码，已排除 `__pycache__`）
- `tests\`（全部测试）
- `docs\`（全部文档）
- `packaging\`（build_windows.ps1 / spec / hooks / runtime_hooks / verify_dist.py）
- `runtime\README_runtime说明.txt`（仅说明文件）

### reports\（16 份）
- v0.17 三大关键报告：`phase2_6_pkg_c_test2_report.md`、`phase2_6_pkgb_hidpi_hotfix_report.md`、`final_requirements_reaudit.md`
- Phase 1 / 2.2 / 2.3 / 2.4 / 2.5 / 2.6 最终报告（2.2F、2C、2D、3B、3C、4A、4B、4C-B、4C-C、5A、5B、6-A）

### checksums\
- `SHA256.txt`（见 §4）

## 4. SHA256

```
文件:  TJU_Info_Retrieval_v0.17-test2_win64.zip
SHA256: F18E85237005B21B05320B27AF4B34BA7E2DCCC68BB805BE3C53E30A5B7AD3DF
生成:  2026-09-09
版本:  v0.17-test2
```

复制前后均校验一致（`hashlib.sha256` 实算，与任务书指定值完全匹配）。

## 5. 运行验证

从 `extracted\TJU_Info_Retrieval_v0.17-test2\` 执行 `信息自动检索整理系统.exe`：

| 验证项 | 结果 |
|--------|------|
| GUI 正常打开（进程启动存活 12s 无崩溃） | PASS（进程级） |
| 标题正确 | 由 PKG-C 阶段人工 GUI 验收确认；version.txt 一致 |
| Demo 可加载 | 由 PKG-C 阶段 GUI PASS 确认（归档前已验证） |
| 关闭正常 | PASS（首次实例可正常终止） |
| 重新启动正常 | PASS（二次启动存活验证通过） |

说明：本归档环境为无桌面会话，GUI 级交互（标题/Demo/窗口交互）此前在
PHASE_2_6_PKG_C 阶段已完成人工验收（GUI PASS）；本次归档冒烟验证覆盖
进程启动存活、依赖完整性（PySide6/Playwright/DLL 齐备）与二次重启。

## 6. 安全扫描

| 扫描项 | 结果 |
|--------|------|
| Cookies / Login Data / Web Data 文件 | 无 |
| storage_state（tju_storage_state.json 等） | 无 |
| token / password / API key 真实值（内容级正则） | 无 |
| Edge profile / BrowserMetrics / Singleton 锁 | 无 |
| .env 及密钥文件 | 无 |
| 个人账号 / 校园登录状态 | 无 |

`runtime\` 仅以说明文件形式保留（README_runtime说明.txt），不复制任何浏览器
配置/登录数据。

## 7. 版本状态

- 版本：**v0.17-test2**
- 状态：**FROZEN（Release Candidate 冻结）**
- 归档日期：2026-09-09
- 归档位置：`F:\TJU_Info_Retrieval_Archive\v0.17_release_candidate\`
- 内容：Windows x64 onedir 包 + 源码快照 + 全部 v0.17 阶段报告 + SHA256 校验

## 8. 未来开发规则

1. 本目录为**冻结归档**，后续任何开发不得直接修改/覆盖本目录内容。
2. 后续版本（v0.18+）开发须在 `E:\AI_Workspace\TJU_Info_Retrieval` 工作区进行，
   完成验证后按同样规范新建归档版本目录。
3. 归档核对依据：`checksums\SHA256.txt`（ZIP 指纹）与 `version.txt`（构建信息）。
4. 运行时所需的天津大学登录态不随归档分发，首次运行需在受控 Edge 中重新登录。

---

—— ARCHIVE COMPLETE（RELEASE ARCHIVE，零功能代码修改）